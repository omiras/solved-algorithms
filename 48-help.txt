function points(games) {
  
  let score = 0;
  
  // 1. Tienes que recorrer todo el array que te pasan en 'games
     // 1.1 Para cada elemento, tienes que fijarte en dos índices del string
     // Por ejemplo "1:0" . En el primer índice, tenemos el número de goles del primer equipo, y en el último
     // índice el número de goles del segundo equipo
     
     // 1.2. Tienes que convertir esos valores a number
     
     // 1.3 Luego tienes que comparar estos dos números y en función del resultado asignar los puntos
      // if x > y: 3 points (win)
//if x < y: 0 points (loss)
//if x = y: 1 point (tie)

  return score;
}
