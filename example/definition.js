// https://www.codewars.com/kata/5715eaedb436cf5606000381


function positiveSum(arr) {
    // Tenemos que sumar todos los elementos del array
      // En la variable "result" voy a acumular el resultado de mi operacion
      // A cada elemento del array compruebo si es positivo o no (es decir, mayor a 0). Si lo es, lo suma en una 
      
    // al final devuelvo la variable "result"
}

console.log(positiveSum([1, 2])); // 3

console.log(positiveSum([1, -2, -7, 10, -8])); // 11

console.log(positiveSum([-1, -2, -7, -10])); // 0 (no hay números positivos! )
